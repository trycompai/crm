# Task specs

Tier 2 task specs live here as `YYYY-MM-DD-<slug>.md` and are committed alongside the change they specify. Format and workflow are defined in `CLAUDE.md` (Part 1 — Orchestration Contract).
